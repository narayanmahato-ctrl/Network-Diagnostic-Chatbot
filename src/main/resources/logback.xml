<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Logback configuration for Network Diagnostic Chatbot -->
    
    <!-- Define properties -->
    <property name="LOG_FILE" value="chatbot.log" />
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n" />

    <!-- Console appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Rolling file appender (rotates when file reaches 10MB) -->
    <appender name="FILE_ROLLING" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE}</file>
        <append>true</append>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
            <fileNamePattern>chatbot.%i.log</fileNamePattern>
            <minIndex>1</minIndex>
            <maxIndex>10</maxIndex>
        </rollingPolicy>
        <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
            <maxFileSize>10MB</maxFileSize>
        </triggeringPolicy>
    </appender>

    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE_ROLLING" />
    </root>

    <!-- Package-specific log levels -->
    <logger name="com.networkbot" level="DEBUG" />
    <logger name="com.networkbot.chatbot" level="INFO" />
    <logger name="com.networkbot.diagnostics" level="INFO" />
    <logger name="com.networkbot.commands" level="DEBUG" />
    <logger name="com.networkbot.utils" level="DEBUG" />
</configuration>
